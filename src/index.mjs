#!/usr/bin/env node
import http from 'node:http';
import os from 'node:os';
import process from 'node:process';
import { loadConfig } from './config.mjs';
import { pollEasySmart } from './tplink-easy-smart.mjs';
import { ping, dnsProbe, wifiLink, fetchWitness } from './probes.mjs';
import { computeCounterDeltas, diagnose, FaultLatch } from './diagnose.mjs';
import { Storage } from './storage.mjs';
import { diagnoseWithAI } from './ai.mjs';

const { config, configPath, dataDir } = loadConfig();
if (process.argv.includes('--check-config')) { console.log(`OK: ${config.name}; ${(config.switches||[]).length} switches, ${(config.nodes||[]).length} nodes, ${(config.links||[]).length} links.`); process.exit(0); }
const storage=new Storage(dataDir),creds={username:process.env.TPLINK_SWITCH_USER||'',password:process.env.TPLINK_SWITCH_PASSWORD||''};
if((config.switches||[]).length&&(!creds.username||!creds.password))throw new Error('TPLINK_SWITCH_USER and TPLINK_SWITCH_PASSWORD are required in .env');
const latch=new FaultLatch(config.debounce?.failures??3,config.debounce?.recoveries??2),previousCounters=new Map(),previousWitnessBoot=new Map();let polling=false;
let current={site:config.name,startedMs:Date.now(),pollStartedMs:null,pollFinishedMs:null,switches:{},interfaces:{},nodes:{},witnesses:{},wifi:null,rawDiagnoses:[],diagnoses:[],configPath};
function ifaceAddress(name){const explicit=config.observer?.interfaceAddresses?.[name];if(explicit)return explicit;return(os.networkInterfaces()[name]||[]).find(x=>x.family==='IPv4'&&!x.internal)?.address||null}
async function pollSwitch(sw){try{return[sw.id,{ok:true,name:sw.name,host:sw.host,ports:await pollEasySmart({host:sw.host,...creds})}]}catch(err){return[sw.id,{ok:false,name:sw.name,host:sw.host,error:String(err?.message||err),ports:[]}]}}
async function pollInterface(name){if(!name)return null;const address=ifaceAddress(name),router=await ping(config.observer.router,name),internet=await Promise.all((config.observer.internetTargets||[]).map(async target=>({target,...await ping(target,name)}))),dns=await dnsProbe(config.observer.dnsServer,address).catch(err=>({ok:false,error:String(err)}));return[name,{configured:true,address,router,internet,dns}]}
async function pollNode(n){if(!n.host||n.probe!=='ping')return[n.id,{name:n.name,skipped:true}];return[n.id,{name:n.name,host:n.host,...await ping(n.host)}]}
async function pollWitness(w){const r=await fetchWitness(w),enriched={...r,name:w.name,expectedClosestAp:w.expectedClosestAp||null,unreliable:w.unreliable!==false};if(r.ok&&r.bootId){const prev=previousWitnessBoot.get(w.id);if(prev&&prev!==r.bootId)storage.event('witness','info',`witness:${w.id}:reboot`,`${w.name} rebooted`,{previousBootId:prev,bootId:r.bootId});previousWitnessBoot.set(w.id,r.bootId)}return[w.id,enriched]}
async function pollCycle(){if(polling)return;polling=true;const started=Date.now();try{const switches=Object.fromEntries(await Promise.all((config.switches||[]).map(pollSwitch)));computeCounterDeltas(switches,previousCounters);const names=[...new Set([config.observer?.ethernetInterface,config.observer?.wifiInterface].filter(Boolean))],interfaces=Object.fromEntries((await Promise.all(names.map(pollInterface))).filter(Boolean)),nodes=Object.fromEntries(await Promise.all((config.nodes||[]).map(pollNode))),witnesses=Object.fromEntries(await Promise.all((config.witnesses||[]).map(pollWitness))),wifi=config.observer?.wifiInterface?await wifiLink(config.observer.wifiInterface):null,candidate={switches,interfaces,nodes,witnesses,wifi},rawDiagnoses=diagnose(config,candidate),latched=latch.update(rawDiagnoses);for(const t of latched.transitions){if(t.type==='started')storage.event('fault',t.item.severity,t.item.id,t.item.title,t.item);else storage.event('recovery','info',t.item.id,`Recovered: ${t.item.title}`,t.item)}current={...current,...candidate,pollStartedMs:started,pollFinishedMs:Date.now(),rawDiagnoses,diagnoses:latched.active}}catch(err){storage.event('supervisor','warning','supervisor:poll-error',`Polling cycle failed: ${err?.message||err}`);current={...current,pollStartedMs:started,pollFinishedMs:Date.now(),supervisorError:String(err?.stack||err)}}finally{polling=false}}
function json(res,value,status=200){const body=JSON.stringify(value);res.writeHead(status,{'content-type':'application/json; charset=utf-8','cache-control':'no-store'});res.end(body)}
function text(res,body,type='text/plain; charset=utf-8',status=200){res.writeHead(status,{'content-type':type,'cache-control':'no-store'});res.end(body)}
function readBody(req,max=65536){return new Promise((resolve,reject)=>{let s='';req.setEncoding('utf8');req.on('data',c=>{s+=c;if(s.length>max){reject(new Error('request too large'));req.destroy()}});req.on('end',()=>resolve(s));req.on('error',reject)})}

const HTML=String.raw`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Network supervisor</title><style>:root{font-family:system-ui,-apple-system,Segoe UI,sans-serif;color-scheme:light dark;--bg:#101418;--card:#181f26;--fg:#edf2f5;--muted:#9dabba;--line:#303b46;--crit:#ff7474;--warn:#f1bd58;--ok:#6fd19a}@media(prefers-color-scheme:light){:root{--bg:#f4f6f8;--card:#fff;--fg:#111820;--muted:#687581;--line:#d8dfe5;--crit:#a51616;--warn:#825400;--ok:#176b3c}}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--fg)}main{max-width:1200px;margin:auto;padding:18px}h1{font-size:1.4rem;margin:0}.top{display:flex;gap:14px;align-items:center;flex-wrap:wrap}.muted{color:var(--muted)}.status{font-weight:700}.ok{color:var(--ok)}.critical{color:var(--crit)}.warning{color:var(--warn)}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(320px,1fr));gap:12px;margin-top:14px}.card{background:var(--card);border:1px solid var(--line);border-radius:10px;padding:12px}.card h2{font-size:1rem;margin:0 0 8px}.fault{border-left:5px solid var(--line);margin:8px 0;padding:8px}.fault.critical{border-left-color:var(--crit)}.fault.warning{border-left-color:var(--warn)}table{width:100%;border-collapse:collapse;font-size:.88rem}td,th{text-align:left;border-bottom:1px solid var(--line);padding:5px}button,textarea{font:inherit}button{padding:7px 10px;border:1px solid var(--line);border-radius:7px;background:var(--card);color:var(--fg);cursor:pointer}textarea{width:100%;min-height:76px;background:var(--bg);color:var(--fg);border:1px solid var(--line);border-radius:7px;padding:8px}pre{white-space:pre-wrap;overflow-wrap:anywhere}.small{font-size:.82rem}</style></head><body><main><div class="top"><h1 id="title">Network supervisor</h1><span id="overall" class="status">Loading…</span><button onclick="pollNow()">Poll now</button><span id="stamp" class="muted small"></span></div><div id="faults"></div><div class="grid"><section class="card"><h2>Managed Ethernet</h2><div id="switches"></div></section><section class="card"><h2>Functional paths</h2><div id="paths"></div></section><section class="card"><h2>Wi-Fi witnesses</h2><div id="witnesses"></div></section><section class="card"><h2>Diagnosis / observation</h2><textarea id="problem" placeholder="Optional human observation, e.g. ‘phone shows Wi-Fi but pages do not load’"></textarea><p><button onclick="saveObservation()">Save observation</button> <button onclick="askAI()">Ask configured AI</button></p><pre id="ai" class="small muted"></pre></section></div><script>
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function api(url,opt){const r=await fetch(url,opt),d=await r.json();if(!r.ok)throw new Error(d.error||r.statusText);return d}async function load(){try{const d=await api('/api/status');document.getElementById('title').textContent=d.site;const faults=d.diagnoses||[],highest=faults.find(x=>x.severity==='critical')?'critical':faults.find(x=>x.severity==='warning')?'warning':'ok',ov=document.getElementById('overall');ov.className='status '+highest;ov.textContent=faults.length?faults.length+' active diagnosis'+(faults.length===1?'':'es'):'Network appears healthy';document.getElementById('stamp').textContent=d.pollFinishedMs?'Last poll '+new Date(d.pollFinishedMs).toLocaleString():'';document.getElementById('faults').innerHTML=faults.map(f=>'<div class="card fault '+esc(f.severity)+'"><b>'+esc(f.title)+'</b><div class="small">'+esc(f.detail)+'</div></div>').join('');const specs=Object.fromEntries((d.configSummary.switches||[]).map(s=>[s.id,s]));document.getElementById('switches').innerHTML=Object.entries(d.switches||{}).map(([id,sw])=>{if(!sw.ok)return '<p><b>'+esc(sw.name)+'</b>: <span class="warning">poll failed</span> '+esc(sw.error)+'</p>';const spec=specs[id]||{};return '<p><b>'+esc(sw.name)+'</b> <span class="muted">'+esc(sw.host)+'</span></p><table><tr><th>Port</th><th>Use</th><th>Link</th><th>Bad Δ</th></tr>'+sw.ports.map(p=>'<tr><td>'+p.port+'</td><td>'+esc(spec.ports?.[String(p.port)]?.name||'')+'</td><td>'+esc(p.link)+'</td><td>'+esc(p.badDelta)+'</td></tr>').join('')+'</table>'}).join('');document.getElementById('paths').innerHTML=Object.entries(d.interfaces||{}).map(([n,x])=>'<p><b>'+esc(n)+'</b> '+esc(x.address||'')+'<br>router '+(x.router?.ok?'✓':'✗')+' · Internet '+((x.internet||[]).some(y=>y.ok)?'✓':'✗')+' · DNS '+(x.dns?.ok?'✓':'✗')+'</p>').join('')+(d.wifi?'<p class="small muted">wlan: '+esc(d.wifi.ssid||'')+' · '+esc(d.wifi.bssid||'')+' · '+esc(d.wifi.signalDbm??'?')+' dBm</p>':'');document.getElementById('witnesses').innerHTML=Object.entries(d.witnesses||{}).map(([id,w])=>'<p><b>'+esc(w.name||id)+'</b>: '+(w.ok?'✓':'unavailable')+(w.ok?'<br><span class="small muted">'+esc(w.hostname||'')+' · BSSID '+esc(w.bssid||'?')+' · '+esc(w.signalDbm??'?')+' dBm · uptime '+esc(w.uptimeSeconds??'?')+' s</span>':'')+'</p>').join('')||'<span class="muted">No witnesses configured.</span>'}catch(e){document.getElementById('overall').textContent=e.message}}async function pollNow(){await api('/api/poll',{method:'POST'});setTimeout(load,500)}async function saveObservation(){await api('/api/observe',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text:document.getElementById('problem').value})});document.getElementById('ai').textContent='Observation saved.'}async function askAI(){const out=document.getElementById('ai');out.textContent='Asking…';try{const d=await api('/api/ai-diagnose',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({problem:document.getElementById('problem').value})});out.textContent=typeof d.answer==='string'?d.answer:JSON.stringify(d.answer,null,2)}catch(e){out.textContent=e.message}}load();setInterval(load,15000);</script></main></body></html>`;
const host=process.env.WEB_HOST||'0.0.0.0',port=Math.max(1,Math.min(65535,Number(process.env.WEB_PORT||8790))),server=http.createServer(async(req,res)=>{try{const url=new URL(req.url,`http://${req.headers.host||'localhost'}`);if(req.method==='GET'&&url.pathname==='/')return text(res,HTML,'text/html; charset=utf-8');if(req.method==='GET'&&url.pathname==='/api/status')return json(res,{...current,configSummary:{switches:config.switches,nodes:config.nodes,links:config.links,witnesses:config.witnesses}});if(req.method==='GET'&&url.pathname==='/api/events')return json(res,storage.recentEvents(Math.min(500,Number(url.searchParams.get('limit')||100))));if(req.method==='POST'&&url.pathname==='/api/poll'){pollCycle();return json(res,{ok:true,polling:true})}if(req.method==='POST'&&url.pathname==='/api/observe'){const body=JSON.parse(await readBody(req)||'{}');storage.observation(body.text);return json(res,{ok:true})}if(req.method==='POST'&&url.pathname==='/api/ai-diagnose'){const body=JSON.parse(await readBody(req)||'{}');return json(res,{answer:await diagnoseWithAI({config,current,storage,problem:body.problem})})}return json(res,{error:'not found'},404)}catch(err){return json(res,{error:String(err?.message||err)},500)}});
server.listen(port,host,()=>console.log(`Network supervisor: http://${host}:${port}/ — ${config.name}`));await pollCycle();setInterval(pollCycle,Math.max(10,Number(config.pollIntervalSeconds||30))*1000).unref();
